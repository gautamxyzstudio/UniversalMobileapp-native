import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CandidateCard = () => {
  return (
    <View>
      <Text>CandidateCard</Text>
    </View>
  );
};

export default CandidateCard;

const styles = StyleSheet.create({});
